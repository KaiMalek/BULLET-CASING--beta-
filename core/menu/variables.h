#pragma once
#include <unordered_map>

namespace variables {
	inline bool active = true;
	inline bool active_panel = false;

	inline float menu_color[4] = { 0 / 255.f, 143 / 255.f, 255 / 255.f, 255 / 255.f }; // R G B A  ||  255 250 50 255
};